# Multi-Subscriber Performance Test
# Single publisher to multiple subscribers

[coordinator]
output_dir = "./results"
output_formats = ["console", "json", "csv"]

[scenario]
name = "multi_subscriber"
description = "Multi-subscriber test: 1 -> 4"
warmup_secs = 2
test_duration_secs = 10
cooldown_secs = 1

[module.coordinator]

[module.publisher_perf]
topic = "perf.test"
message_count = 100000
message_size = 100
concurrent_tasks = 1

[module.subscriber_perf]
topic = "perf.test"
concurrent_tasks = 4

[message-router]
[[message-router.route]]
pattern = "perf.#"
bus = "internal"

[message-bus.internal]
class = "in-memory"
